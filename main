<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Your Name — Portfolio</title>
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
  body, html { margin:0; padding:0; font-family:"Helvetica Neue", Helvetica, Arial, sans-serif; background:#fff; color:#000; }
  a { color:#000; text-decoration:none; transition:0.2s; }
  a:hover { opacity:0.7; }

  header { padding:2rem; text-align:center; border-bottom:1px solid #000; }
  header h1 { margin:0; font-size:2rem; letter-spacing:2px; text-transform:uppercase; }
  nav { margin-top:0.5rem; }
  nav a { margin: 0 1rem; font-size:0.9rem; }

  main { max-width:1200px; margin:2rem auto; padding:0 1rem; display:grid; grid-template-columns: repeat(auto-fit, minmax(300px,1fr)); gap:1.5rem; }

  .post { display:flex; flex-direction:column; overflow:hidden; position:relative; }
  .post img { width:100%; height:auto; display:block; transition: transform 0.3s ease; }
  .post img:hover { transform: scale(1.05); }

  .caption { position:absolute; bottom:0; left:0; width:100%; background:rgba(255,255,255,0.9); color:#000; padding:0.5rem 1rem; font-size:0.85rem; opacity:0; transition: opacity 0.3s ease; }
  .post:hover .caption { opacity:1; }

  footer { text-align:center; padding:2rem; font-size:0.8rem; border-top:1px solid #000; }

  @media (max-width:600px){
    main { grid-template-columns: 1fr; }
    .caption { font-size:0.75rem; }
  }
</style>
</head>
<body>

<header>
  <h1>Your Name</h1>
  <nav>
    <a href="#home">Home</a>
    <a href="#portfolio">Portfolio</a>
    <a href="#contact">Contact</a>
  </nav>
</header>

<main id="portfolio">
  <div class="post">
    <img src="images/art1.jpg" alt="Artwork 1">
    <div class="caption">Artwork 1 — Medium, Year</div>
  </div>

  <div class="post">
    <img src="images/art2.jpg" alt="Artwork 2">
    <div class="caption">Artwork 2 — Medium, Year</div>
  </div>

  <div class="post">
    <img src="images/art3.jpg" alt="Artwork 3">
    <div class="caption">Artwork 3 — Medium, Year</div>
  </div>

  <!-- Add more artworks here -->
</main>

<footer>
  &copy; 2026 Your Name — All rights reserved.
</footer>

</body>
</html>
